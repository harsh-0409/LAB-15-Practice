name: CI Build and Push Docker Images

on:
  push:
    branches: [ main ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      # 1. Checkout source
      - name: Checkout Repository
        uses: actions/checkout@v4

      # 2. Set up Docker Buildx
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # 3. Login to Docker Hub
      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}  # define in repo settings
          password: ${{ secrets.DOCKER_SECRET }}    # define in repo settings

      # 4. Build & push backend image
      - name: Build and Push Backend Image
        uses: docker/build-push-action@v6
        with:
          context: ./travel-backend-main/travel-backend-main
          file: ./travel-backend-main/backend.dockerfile
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/travel-backend-practice:latest

      # 5. Build & push frontend image
      - name: Build and Push Frontend Image
        uses: docker/build-push-action@v6
        with:
          context: ./travel-frontend-main/travel-frontend-main
          file: ./travel-frontend-main/frontend.dockerfile
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/travel-frontend-practice:latest
